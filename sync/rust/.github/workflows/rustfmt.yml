name: rustfmt

on:
  push:
    branches:
      - master
  pull_request:
    branches:
      - master

jobs:
  rustfmt:
    uses: chrisliebaer/.github/.github/workflows/rustfmt.yml@c7b49aac6c730cdd2b84a7be4391eeaa43a18ef6 # master
